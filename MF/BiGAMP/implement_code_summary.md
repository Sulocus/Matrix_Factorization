# Code Implementation Progress Summary
*Accumulated implementation progress for all files*


================================================================================
## IMPLEMENTATION File big_amp_repro/README.md; ROUND 0 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:07:19
**File Implemented**: big_amp_repro/README.md

**Core Purpose**:
- Provides the primary documentation and project overview for the Bilinear Generalized Approximate Message Passing (BiG-AMP) reproduction codebase.

**Public Interface**:
- N/A (Markdown documentation file).

**Internal Dependencies**:
- None.

**External Dependencies**:
- None.

**Implementation Notes**:
- **Architecture decisions**: Initialized as a placeholder to establish the project root and structure according to the reproduction plan.
- **Cross-File Relationships**: Serves as the high-level guide for the entire repository, eventually detailing how to run experiments and the purpose of each module (`core/`, `estimators/`, `apps/`, etc.).

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/core/base_solver.py; ROUND 1 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:07:33
**File Implemented**: big_amp_repro/core/base_solver.py

**Core Purpose**:
- Defines the abstract interface and common utility methods for BiG-AMP solvers to ensure consistency across different implementation variants (e.g., scalar vs. matrix variance).

**Public Interface**:
- Class `BaseBiGAMP`: Abstract base class for bilinear inference solvers. | Key methods: `solve(Y, mask)`, `_check_convergence(current_val, prev_val)` | Constructor params: `params` (dict containing `nit`, `tol`, `verbose`, `damping`).

**Internal Dependencies**:
- External packages: `numpy` - numerical operations and norm calculations; `abc` - defining abstract base classes.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/core/engine.py` (concrete implementation).
- Key exports used elsewhere: `BaseBiGAMP` class.

**Implementation Notes**:
- Architecture decisions: Uses an abstract method `solve` to force child classes to implement the specific BiG-AMP iteration logic (Table III/IV).
- Cross-File Relationships: Acts as the parent for the main computational engine and provides a unified configuration structure for application wrappers in the `apps/` directory.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/core/engine.py; ROUND 2 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:07:59
**File Implemented**: big_amp_repro/core/engine.py

**Core Purpose**:
- Implements the Scalar Variance BiG-AMP algorithm (Table IV of the paper), providing a computationally efficient $O(N|\Omega|)$ solver for bilinear inference problems like Matrix Completion and Dictionary Learning.

**Public Interface**:
- Class `BiGAMPEngine`: The primary solver engine for BiG-AMP iterations.
    - Key methods:
        - `solve(Y, mask=None)`: Executes the 16-step BiG-AMP loop to estimate matrices $A$ and $X$ given observations $Y$. Returns a dictionary containing estimates (`a_hat`, `x_hat`, `z_hat`), final variances, and iteration count.
    - Constructor params:
        - `est_a`: Estimator object for matrix $A$.
        - `est_x`: Estimator object for matrix $X$.
        - `est_z`: Estimator object for the likelihood $p(Y|Z)$.
        - `params`: Dictionary containing `rank`, `tol`, `nit`, and initialization values.

**Internal Dependencies**:
- From `.base_solver`: `BaseBiGAMP` (Inheritance).
- External packages: `numpy` - Used for vectorized matrix algebra and Frobenius norm calculations.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/apps/matrix_completion.py`, `big_amp_repro/apps/robust_pca.py`, and `big_amp_repro/apps/dict_learning.py`.
- Key exports used elsewhere: `BiGAMPEngine` class for executing the inference loop.

**Implementation Notes**:
- **Architecture decisions**: Implements the "Scalar Variance" version of the algorithm, which averages variances across dimensions to reduce computational complexity from $O(N^2)$ to $O(N)$ per entry.
- **Cross-File Relationships**: The engine acts as a coordinator that passes intermediate messages (`p_hat`, `r_hat`, `q_hat`) to external Estimator objects, which are responsible for computing posterior means and variances based on specific priors (e.g., Gaussian, Bernoulli-Gaussian).
- **Damping**: Includes basic damping on the residual `s_hat` and the estimates `a_hat`/`x_hat` to ensure numerical stability.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/estimators/gaussian.py; ROUND 3 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:08:27
**File Implemented**: big_amp_repro/estimators/gaussian.py

**Core Purpose**:
- Defines the base interface for BiG-AMP estimators and provides standard Gaussian-based implementations for priors and likelihoods (PIAWGN).

**Public Interface**:
- Class `BaseEstimator`: Abstract base class for all estimators. | Key methods: `estimate(mean_in, var_in)` | Constructor params: N/A
- Class `GaussianEstimator`: Implements Gaussian prior estimation for variables $A$ and $X$. | Key methods: `estimate(mean_in, var_in)` | Constructor params: `mean_prior` (float), `var_prior` (float)
- Class `PIAWGNEstimator`: Implements the Possibly Incomplete Additive White Gaussian Noise likelihood for the $Z$ update, supporting masked observations. | Key methods: `estimate(p_hat, var_p)` | Constructor params: `Y` (np.ndarray), `mask` (np.ndarray, optional), `var_noise` (float)

**Internal Dependencies**:
- External packages: `numpy` - Used for vectorized matrix calculations and masking operations.
- External packages: `abc` - Used to define the abstract base class structure.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/core/engine.py` (for the main BiG-AMP loop), `big_amp_repro/apps/matrix_completion.py`, and `big_amp_repro/apps/dict_learning.py`.
- Key exports used elsewhere: `BaseEstimator` (interface), `GaussianEstimator`, `PIAWGNEstimator`.

**Implementation Notes**:
- **Architecture decisions**: The `PIAWGNEstimator` handles missing data by returning the input mean and variance ($p\_hat, \nu_p$) for unobserved entries (where `mask` is False), effectively treating them as having infinite noise variance.
- **Cross-File Relationships**: These estimators are passed as objects to the `BiGAMP` engine, which calls their `estimate` methods during the R-steps (R5-R8 for $Z$, R13-R16 for $A$ and $X$).

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/estimators/bernoulli_bg.py; ROUND 4 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:08:46
**File Implemented**: big_amp_repro/estimators/bernoulli_bg.py

**Core Purpose**:
- Implements the Bernoulli-Gaussian (BG) estimator used to model sparse priors in bilinear inference problems. It calculates the posterior mean and scalar variance for variables assumed to be zero with probability $(1-\lambda)$ or Gaussian-distributed with probability $\lambda$.

**Public Interface**:
- Class `BernoulliGaussianEstimator`: Implements the sparse prior mapping for BiG-AMP iterations.
    - Key methods:
        - `estimate(mean_in, var_in)`: Computes the posterior mean and average variance given the BiG-AMP "observations" ($r$ or $q$) and their associated variances.
    - Constructor params:
        - `sparsity_rate` (float): The prior probability $\lambda$ of a component being non-zero.
        - `var_prior` (float): The variance of the Gaussian component of the prior.

**Internal Dependencies**:
- From `.gaussian`: `BaseEstimator` (Abstract base class).
- External packages: `numpy` - Used for vectorized numerical computations and log-sum-exp stability tricks.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/apps/dict_learning.py` (for sparse coefficients) and `big_amp_repro/apps/robust_pca.py` (for the sparse outlier matrix).
- Key exports used elsewhere: `pi_hat` (posterior activity probability) is stored as an attribute for use by `big_amp_repro/utils/em_tuner.py` to update the sparsity rate $\lambda$.

**Implementation Notes**:
- **Numerical Stability**: Uses a log-ratio formulation and `np.clip` when calculating the posterior activity probability $\pi$ to prevent overflow/underflow during exponential operations.
- **Scalar Variance**: Consistent with the Scalar-Variance BiG-AMP (Table IV), the method returns the mean of the element-wise posterior variances.
- **State Retention**: Stores `self.pi_hat` internally, which is a requirement for the Expectation-Maximization (EM) parameter tuning phase described in the paper.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/estimators/mixture.py; ROUND 5 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:09:33
**File Implemented**: big_amp_repro/estimators/mixture.py

**Core Purpose**:
- Provides advanced non-Gaussian estimators for priors and likelihoods, specifically Gaussian Mixture Models (GMM) and Laplacian distributions. These are essential for handling sparsity, outliers, and heavy-tailed noise in applications like Robust PCA and Matrix Completion.

**Public Interface**:
- **Class `GaussianMixtureEstimator`**: Implements a GMM prior $p(x) = \sum w_k \mathcal{N}(x; \mu_k, \sigma^2_k)$. | Key methods: `estimate(mean_in, var_in)` | Constructor params: `weights`, `means`, `vars_prior`.
- **Class `LaplacianEstimator`**: Implements a Laplacian prior $p(x) = \frac{1}{2b} \exp(-\frac{|x-\mu|}{b})$ using MMSE estimation. | Key methods: `estimate(mean_in, var_in)` | Constructor params: `scale`, `mean`.
- **Class `PIAWLNEstimator`**: Possibly Incomplete Additive White Laplacian Noise likelihood for matrix completion with outliers. | Key methods: `estimate(p_hat, var_p)` | Constructor params: `Y`, `mask`, `scale`.

**Internal Dependencies**:
- From `.gaussian`: `BaseEstimator` (Abstract base class).
- External packages: `numpy` (vectorized math), `scipy.special.erfcx` (numerically stable scaled complementary error function).

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/apps/matrix_completion.py` (for MovieLens experiments) and `big_amp_repro/apps/robust_pca.py`.
- Key exports used elsewhere: `estimate` method is called by the BiG-AMP loop in `core/engine.py`.

**Implementation Notes**:
- **Numerical Stability**: Uses `erfcx` for Laplacian estimation to prevent overflow/underflow in exponential ratios and `log-sum-exp` logic for GMM posterior weight calculations.
- **Variance Approximation**: For the `LaplacianEstimator` and `PIAWLNEstimator`, the input variance is returned as the output variance. This is a common stabilization technique in BiG-AMP to prevent the "variance explosion" often associated with the complex exact MMSE variance of non-smooth priors.
- **Law of Total Variance**: The GMM estimator correctly implements the Law of Total Variance to combine component-wise variances and the variance of component means.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/core/damping.py; ROUND 6 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:09:53
**File Implemented**: big_amp_repro/core/damping.py

**Core Purpose**
- Implements the adaptive damping mechanism and Bethe Free Energy approximation ($J(t)$) to ensure numerical stability and convergence of the BiG-AMP algorithm, especially in ill-conditioned or non-convex scenarios.

**Public Interface**
- **Class `AdaptiveDamping`**: Manages the damping factor $\beta$ and step acceptance logic based on cost evolution.
    - **Key methods**:
        - `compute_cost(z_hat, p_hat, var_p, var_z, y, mask=None)`: Calculates a simplified Bethe Free Energy approximation using quadratic penalties and log-variance terms.
        - `update_step_size(current_cost)`: Adjusts $\beta$ (increase on success, decrease on failure) and returns a boolean indicating if the current iteration's step should be accepted.
        - `apply_damping(current_val, prev_val)`: Performs the linear combination: $\beta \cdot \text{current} + (1-\beta) \cdot \text{previous}$.
    - **Constructor params**: `initial_beta` (default 0.5), `beta_min` (0.01), `beta_max` (1.0), `beta_inc` (1.1), `beta_dec` (0.5), `threshold` (1e-4).

**Internal Dependencies**
- External packages: `numpy` - Used for vectorized matrix operations and logarithmic cost calculations.

**External Dependencies**
- Expected to be imported by: `big_amp_repro/core/engine.py` (to stabilize the 16-step BiG-AMP loop).
- Key exports used elsewhere: `AdaptiveDamping` class for stateful tracking of the damping factor across iterations.

**Implementation Notes**
- **Architecture decisions**: The cost function $J(t)$ is simplified for AWGN (Additive White Gaussian Noise) cases as suggested in Section IV-C of the paper, focusing on the residual between the posterior estimate $\hat{z}$ and the transform-domain mean $\hat{p}$.
- **Cross-File Relationships**: This module acts as a regulator for the `BigAMPEngine`. While the engine computes the message passing updates, `AdaptiveDamping` decides how much of that update to keep to prevent divergence.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/utils/em_tuner.py; ROUND 7 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:10:11
**File Implemented**: big_amp_repro/utils/em_tuner.py

**Core Purpose**:
- Implements the Expectation-Maximization (EM) update rules for automated parameter tuning in BiG-AMP, specifically for noise variance, prior variances, and sparsity rates as described in Section V-A of the paper.

**Public Interface**:
- **Class `EMTuner`**: A utility class providing static and class methods for hyperparameter optimization.
    - `update_noise_var(estimator_z, z_hat, var_z, Y, mask=None)`: Updates the noise variance ($\nu_w$) for PIAWGN likelihoods based on observed residuals and posterior variance.
    - `update_prior_vars(estimator, hat_val, var_val)`: Updates the prior variance ($\nu_a$ or $\nu_x$) for Gaussian or Bernoulli-Gaussian priors.
    - `update_sparsity_rate(estimator)`: Updates the sparsity parameter ($\lambda$) for Bernoulli-Gaussian estimators using the posterior activity probabilities ($\hat{\pi}$).
    - `tune_all(engine_state, est_a, est_x, est_z, Y, mask=None)`: A unified entry point that extracts state from the BiG-AMP engine and updates all relevant hyperparameters in one call.

**Internal Dependencies**:
- External packages: `numpy` - Used for vectorized matrix operations and statistical calculations (mean, sum).

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/core/engine.py` (during the iteration loop) and application wrappers in `big_amp_repro/apps/`.
- Key exports used elsewhere: `EMTuner.tune_all` is the primary interface for automated tuning.

**Implementation Notes**:
- **Numerical Stability**: Implements floor constraints (e.g., `1e-10` for variances) to prevent numerical collapse during EM iterations.
- **Flexibility**: Methods are designed to handle both scalar and matrix-valued variances, ensuring compatibility with the Scalar Variance BiG-AMP implementation.
- **Duck Typing**: Uses `hasattr` checks to determine if an estimator supports specific parameters (like `var_noise` or `sparsity_rate`), allowing it to work across different estimator types without strict inheritance requirements.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/utils/rank_logic.py; ROUND 8 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:10:33
**File Implemented**: big_amp_repro/utils/rank_logic.py

**Core Purpose**:
Provides logic for dynamic rank adjustment and model selection in BiG-AMP, specifically implementing Rank Contraction via SVD and the corrected Akaike Information Criterion (AICc) to determine the optimal inner dimension $N$.

**Public Interface**:
- Class `RankManager`: A utility class for rank-related operations.
    - Method `contract_rank(a_hat, x_hat, threshold=0.05)`: Reduces the rank of the system by performing SVD on $\hat{\mathbf{X}}$, truncating small singular values, and rotating $\hat{\mathbf{A}}$ to preserve the bilinear product. -> Returns `(a_hat_new, x_hat_new, new_rank)`.
    - Method `compute_aicc(y, z_hat, mask, rank, m, l)`: Calculates the AICc score based on Gaussian log-likelihood and the degrees of freedom $k = r(M+L-r)$. -> Returns `float` (AICc score).
    - Method `estimate_rank_aic(y, mask, max_rank, solver_factory)`: Performs a heuristic sweep across possible ranks to find the one that minimizes AICc. -> Returns `(best_rank, results_dict)`.

**Internal Dependencies**:
- External packages: `numpy` (matrix operations), `scipy.linalg.svd` (singular value decomposition).

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/apps/matrix_completion.py`, `big_amp_repro/apps/robust_pca.py`, and `big_amp_repro/apps/dict_learning.py`.
- Key exports used elsewhere: `RankManager.contract_rank` for adaptive rank reduction during EM iterations.

**Implementation Notes**:
- **Architecture decisions**: Rank contraction uses a relative threshold against the largest singular value to ensure scale-invariant pruning.
- **Cross-File Relationships**: This utility complements `EMTuner` by providing a way to adjust the model complexity (rank) while `EMTuner` adjusts the statistical parameters (variances).
- **Error Handling**: Includes a fallback for SVD convergence failures and checks for degrees of freedom in AICc to avoid division by zero.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/utils/metrics.py; ROUND 9 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:10:46
**File Implemented**: big_amp_repro/utils/metrics.py

**Core Purpose**:
- Provides standardized evaluation metrics and statistical utilities to assess the performance of BiG-AMP recovery, specifically focusing on error measurement (NMSE, NMAE) and phase transition analysis.

**Public Interface**:
- `compute_nmse(y_true, y_pred, mask=None)`: Calculates the Normalized Mean Squared Error in decibels (dB). -> `float`: The NMSE value.
- `compute_nmae(y_true, y_pred, mask=None)`: Calculates the Normalized Mean Absolute Error, primarily used for MovieLens benchmarks. -> `float`: The NMAE value.
- `compute_snr(signal, noise)`: Calculates the Signal-to-Noise Ratio in dB. -> `float`: The SNR value.
- `is_success(nmse, threshold=-60)`: Determines if a recovery attempt is considered successful based on an NMSE threshold. -> `bool`: True if successful.
- `get_phase_transition_stats(results_grid, threshold=-60)`: Converts a matrix of NMSE results into a binary success/failure grid for phase transition plotting. -> `np.ndarray`: Binary grid.

**Internal Dependencies**:
- External packages: `numpy` - Used for vectorized matrix operations and logarithmic calculations.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/experiments/run_synthetic_ptc.py`, `big_amp_repro/experiments/run_movielens.py`, and various application wrappers in `big_amp_repro/apps/`.
- Key exports used elsewhere: `compute_nmse` for general validation and `compute_nmae` for matrix completion tasks.

**Implementation Notes**:
- **Masking Support**: Both NMSE and NMAE functions support an optional boolean mask, allowing for error calculation only on observed or unobserved entries (crucial for Matrix Completion).
- **Numerical Stability**: Includes a small epsilon (`1e-20`) in log calculations to prevent division by zero or log of zero errors.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/apps/matrix_completion.py; ROUND 10 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:11:04
**File Implemented**: big_amp_repro/apps/matrix_completion.py

**Core Purpose**:
- Provides a high-level application wrapper for Matrix Completion (MC) as described in Section VI of the BiG-AMP paper, integrating the engine, estimators, EM tuning, and rank contraction logic.

**Public Interface**:
- Class `MatrixCompletion`: Orchestrates the BiG-AMP solver for matrix recovery from partial observations.
    - Key methods:
        - `fit(Y, mask=None, var_noise=1e-3, var_a=1.0, var_x=1.0)`: Executes the BiG-AMP iterations, optionally performing EM parameter updates and rank contraction.
        - `predict()`: Returns the reconstructed matrix product $\hat{A}\hat{X}$.
        - `get_metrics(Y_true, mask_test=None)`: Calculates NMSE and NMAE for validation.
    - Constructor params: `rank` (initial inner dimension), `nit` (max iterations), `tol` (convergence threshold), `use_em` (toggle EM tuning), `use_rank_contraction` (toggle rank reduction), `noise_type` ('gaussian' or 'laplacian').

**Internal Dependencies**:
- From `..core.engine`: `BiGAMPEngine`
- From `..estimators.gaussian`: `GaussianEstimator`, `PIAWGNEstimator`
- From `..estimators.mixture`: `PIAWLNEstimator`
- From `..utils.em_tuner`: `EMTuner`
- From `..utils.rank_logic`: `RankManager`
- From `..utils.metrics`: `compute_nmse`, `compute_nmae`
- External packages: `numpy` - Matrix operations and initialization.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/main.py`, `big_amp_repro/experiments/run_movielens.py`, and `big_amp_repro/experiments/run_synthetic_ptc.py`.
- Key exports used elsewhere: `MatrixCompletion` class for standardized MC experiments.

**Implementation Notes**:
- **Architecture decisions**: Implements an outer loop to handle EM updates and Rank Contraction, while the inner loop (handled by `BiGAMPEngine`) focuses on the message-passing convergence.
- **Noise Handling**: Supports both Gaussian noise (standard MC) and Laplacian noise (robust MC for outliers) by switching between `PIAWGNEstimator` and `PIAWLNEstimator`.
- **Rank Dynamics**: Uses `RankManager` to dynamically reduce the rank during the fitting process if singular values indicate a lower-dimensional structure.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/apps/__init__.py; ROUND 11 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:11:32
**File Implemented**: big_amp_repro/apps/__init__.py

**Core Purpose**
- Serves as the package initializer for the `apps` directory, exposing high-level application wrappers for Matrix Completion, Robust PCA, and Dictionary Learning through a unified interface.

**Public Interface**
- **Class MatrixCompletion**: Wrapper for solving matrix completion problems using BiG-AMP.
- **Class RobustPCA**: Wrapper for decomposing matrices into low-rank and sparse components.
- **Class DictLearning**: Wrapper for learning a dictionary and sparse representation from data.
- **Constants/Types**: `__all__`: List containing the three application classes for clean namespace exports.

**Internal Dependencies**
- From `.matrix_completion`: `MatrixCompletion`
- From `.robust_pca`: `RobustPCA`
- From `.dict_learning`: `DictLearning`

**External Dependencies**
- Expected to be imported by: `big_amp_repro/main.py` and various scripts in `big_amp_repro/experiments/`.
- Key exports used elsewhere: `MatrixCompletion`, `RobustPCA`, `DictLearning`.

**Implementation Notes**
- **Architecture decisions**: Uses the `__init__.py` file to simplify imports, allowing users to import applications directly from `big_amp_repro.apps` rather than deep-nesting into specific sub-modules.
- **Cross-File Relationships**: This file acts as a bridge between the specific application logic (implemented in individual files) and the end-user or experiment scripts.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/apps/robust_pca.py; ROUND 12 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:12:14
**File Implemented**: big_amp_repro/apps/robust_pca.py

**Core Purpose**:
- Implements Robust Principal Component Analysis (RPCA) by formulating the decomposition $Y = L + S + W$ as a bilinear problem using an augmented matrix approach ($Y = [A, I] [X; S]^T + W$). It leverages the BiG-AMP engine to simultaneously estimate the low-rank basis, coefficients, and sparse outliers.

**Public Interface**:
- Class `RobustPCA`: High-level wrapper for performing robust matrix decomposition. | Key methods: `fit(Y, mask)`, `get_low_rank()`, `get_sparse()`, `get_metrics(L_true, S_true)` | Constructor params: `rank`, `nit`, `tol`, `use_em`, `sparsity_rate`, `var_s`, `verbose`.
- Class `RPCAEstimatorA`: Custom estimator for the augmented matrix $[A, I]$, where $I$ is fixed. | Key methods: `estimate(q_hat, nu_q)`.
- Class `RPCAEstimatorX`: Custom estimator for the augmented matrix $[X; S]$, combining Gaussian and Bernoulli-Gaussian priors. | Key methods: `estimate(r_hat, nu_r)`.

**Internal Dependencies**:
- From `..core.engine`: `BiGAMPEngine` - Executes the message passing iterations.
- From `..estimators.gaussian`: `GaussianEstimator`, `PIAWGNEstimator` - Handles low-rank priors and masked likelihood.
- From `..estimators.bernoulli_bg`: `BernoulliGaussianEstimator` - Models the sparse outlier component $S$.
- From `..utils.em_tuner`: `EMTuner` - Provides logic for updating noise and prior variances.
- From `..utils.metrics`: `compute_nmse` - Evaluates recovery quality.
- External packages: `numpy` - Matrix operations and identity matrix generation.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/experiments/run_video_rpca.py` for background/foreground separation and `big_amp_repro/main.py`.
- Key exports used elsewhere: `RobustPCA` class.

**Implementation Notes**:
- **Augmented Formulation**: Implements Equation 127 from the paper, where the effective rank of the BiG-AMP engine is set to $N + M$ (low-rank dimension + number of rows) to accommodate the identity matrix and sparse component.
- **Fixed Identity Logic**: `RPCAEstimatorA` explicitly sets the posterior variance of the identity block to $0.0$, ensuring the engine treats that portion of the basis as known and constant.
- **Manual EM Updates**: Because the augmented matrices have heterogeneous priors (Gaussian vs. BG), the `fit` method performs manual EM parameter updates for the specific sub-blocks of $A$ and $X$.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/apps/dict_learning.py; ROUND 13 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:12:31
**File Implemented**: big_amp_repro/apps/dict_learning.py

**Core Purpose**:
- Provides a high-level application wrapper for Dictionary Learning (DL), solving the bilinear problem $Y = AX + W$ where $X$ is sparse, using the BiG-AMP engine with Bernoulli-Gaussian priors and EM-based parameter tuning.

**Public Interface**:
- Class `DictLearning`: Orchestrates the dictionary learning process. | Key methods: `fit(Y, mask, ...)` (executes BiG-AMP/EM iterations), `predict()` (returns $A \hat{X}$), `get_metrics(A_true, X_true, Y_true)` (calculates NMSE for recovery validation). | Constructor params: `rank` (number of atoms), `nit`, `tol`, `use_em`, `verbose`.

**Internal Dependencies**:
- From `..core.engine`: `BiGAMPEngine` - Core solver.
- From `..estimators.gaussian`: `GaussianEstimator` (for dictionary $A$), `PIAWGNEstimator` (for likelihood $Z$).
- From `..estimators.bernoulli_bg`: `BernoulliGaussianEstimator` (for sparse coefficients $X$).
- From `..utils.em_tuner`: `EMTuner` - Updates noise and prior variances.
- From `..utils.metrics`: `compute_nmse` - Performance evaluation.
- External packages: `numpy` - Matrix operations.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/main.py` and dictionary learning specific experiment scripts.
- Key exports used elsewhere: `DictLearning` class.

**Implementation Notes**:
- **Architecture decisions**: Implements an outer EM loop (10 iterations by default) that calls the BiG-AMP engine for inner iterations, allowing hyperparameters like sparsity rate and noise variance to be refined during the learning process.
- **Cross-File Relationships**: Integrates the `BernoulliGaussianEstimator` specifically for the sparse component $X$, distinguishing this application from Matrix Completion (which uses Gaussian priors).
- **Evaluation**: Includes a `get_metrics` method that acknowledges the inherent permutation and scale invariance of Dictionary Learning.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/core/__init__.py; ROUND 14 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:12:41
**File Implemented**: big_amp_repro/core/__init__.py

**Core Purpose**:
- Serves as the package initializer for the `core` module, providing a centralized entry point for the primary BiG-AMP solver components.
- Simplifies the import hierarchy by exposing the abstract base class, the main engine, and the damping logic at the package level.

**Public Interface**:
- **Class `BaseBiGAMP`**: Abstract base class defining the interface for bilinear solvers.
- **Class `BiGAMPEngine`**: The primary implementation of the Scalar Variance BiG-AMP algorithm (Table IV).
- **Class `AdaptiveDamping`**: Logic for step-size control and Bethe Free Energy cost function calculation.

**Internal Dependencies**:
- From `.base_solver`: `BaseBiGAMP`
- From `.engine`: `BiGAMPEngine`
- From `.damping`: `AdaptiveDamping`

**External Dependencies**:
- **Expected to be imported by**: Application wrappers (`apps/matrix_completion.py`, `apps/robust_pca.py`, `apps/dict_learning.py`) and the main execution script (`main.py`).
- **Key exports used elsewhere**: `BiGAMPEngine` (for running iterations) and `AdaptiveDamping` (for convergence control).

**Implementation Notes**:
- **Architecture decisions**: Uses the `__all__` list to explicitly define the public API, ensuring that only the intended solver components are exposed when using `from big_amp_repro.core import *`.
- **Cross-File Relationships**: This file bridges the gap between the low-level algorithmic implementation in `engine.py` and the high-level application logic in the `apps/` directory.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/estimators/__init__.py; ROUND 15 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:12:52
**File Implemented**: big_amp_repro/estimators/__init__.py

**Core Purpose**
- Serves as the central access point for the `estimators` package, exposing all prior and likelihood estimators used by the BiG-AMP engine to calculate posterior means and variances.

**Public Interface**
- **Class BaseEstimator**: Abstract base class defining the `estimate` interface for all BiG-AMP estimators.
- **Class GaussianEstimator**: Implements Gaussian prior/likelihood updates.
- **Class PIAWGNEstimator**: Implements "Possibly Incomplete Additive White Gaussian Noise" for matrix completion and standard regression.
- **Class BernoulliGaussianEstimator**: Implements sparse priors using the Bernoulli-Gaussian model.
- **Class GaussianMixtureEstimator**: Implements flexible priors/likelihoods via a mixture of Gaussians.
- **Class LaplacianEstimator**: Implements Laplacian priors (often used for Robust PCA) via a zero-mean Gaussian Mixture approximation.
- **Class PIAWLNEstimator**: Implements "Possibly Incomplete Additive White Laplacian Noise" for robust matrix completion.

**Internal Dependencies**
- From `.gaussian`: `BaseEstimator`, `GaussianEstimator`, `PIAWGNEstimator`
- From `.bernoulli_bg`: `BernoulliGaussianEstimator`
- From `.mixture`: `GaussianMixtureEstimator`, `LaplacianEstimator`, `PIAWLNEstimator`

**External Dependencies**
- **Expected to be imported by**: `big_amp_repro/core/engine.py` (for iteration logic), `big_amp_repro/apps/matrix_completion.py`, `big_amp_repro/apps/robust_pca.py`, and `big_amp_repro/apps/dict_learning.py` (for problem setup).
- **Key exports used elsewhere**: All estimator classes are used to define the statistical properties of the matrices $A$, $X$, and the noise $W$.

**Implementation Notes**
- **Architecture decisions**: By centralizing these in `__init__.py`, the rest of the library can import any estimator directly from `big_amp_repro.estimators` without knowing the specific submodule structure.
- **Cross-File Relationships**: This file completes the `estimators` module, bridging the mathematical implementations in the submodules to the application wrappers in `apps/`.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/utils/__init__.py; ROUND 16 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:13:02
**File Implemented**: big_amp_repro/utils/__init__.py

**Core Purpose**:
- Acts as the package initializer for the `utils` subpackage, exposing a unified interface for EM parameter tuning, rank management logic, and performance evaluation metrics.

**Public Interface**:
- Class `EMTuner`: Manages Expectation-Maximization updates for noise and prior variances | Key methods: `update_parameters`
- Class `RankManager`: Handles rank contraction and AICc-based model selection | Key methods: `check_rank_contraction`, `compute_aicc`
- Function `compute_nmse(y_true, y_pred)`: Calculates Normalized Mean Squared Error -> `float`
- Function `compute_nmae(y_true, y_pred)`: Calculates Normalized Mean Absolute Error -> `float`
- Function `compute_snr(signal, noise)`: Calculates Signal-to-Noise Ratio -> `float`
- Function `is_success(nmse, threshold)`: Determines if a recovery attempt was successful based on an NMSE threshold -> `bool`
- Function `get_phase_transition_stats(results)`: Aggregates simulation results for phase transition curve plotting -> `dict`

**Internal Dependencies**:
- From `.em_tuner`: `EMTuner`
- From `.rank_logic`: `RankManager`
- From `.metrics`: `compute_nmse`, `compute_nmae`, `compute_snr`, `is_success`, `get_phase_transition_stats`

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/apps/*.py` (Matrix Completion, RPCA, Dictionary Learning) and `big_amp_repro/experiments/*.py`.
- Key exports used elsewhere: `EMTuner` for automated parameter estimation, `RankManager` for dynamic rank adjustment, and metric functions for validation.

**Implementation Notes**:
- Architecture decisions: Uses the `__init__.py` file to flatten the namespace of the `utils` directory, allowing consumers to import utilities directly from `big_amp_repro.utils` rather than specific sub-modules.
- Cross-File Relationships: This file bridges the gap between the low-level mathematical utilities and the high-level application wrappers.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/experiments/__init__.py; ROUND 17 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:13:09
**File Implemented**: big_amp_repro/experiments/__init__.py

**Core Purpose**:
- Initializes the `experiments` package, serving as the container for scripts that reproduce the specific numerical results and figures from the BiG-AMP paper.

**Public Interface**:
- None (currently an empty package marker).

**Internal Dependencies**:
- None.

**External Dependencies**:
- Expected to be imported by: `big_amp_repro/main.py` or executed as standalone scripts within the package.
- Key exports used elsewhere: None.

**Implementation Notes**:
- Architecture decisions: This file follows standard Python package structure to allow for organized execution of different experimental setups (Synthetic, MovieLens, and Video RPCA).
- Cross-File Relationships: This package will house the high-level scripts that instantiate the application wrappers (from `apps/`) and the core engine (from `core/`) to generate the data for the paper's figures.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/experiments/run_synthetic_ptc.py; ROUND 18 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:13:24
**File Implemented**: big_amp_repro/experiments/run_synthetic_ptc.py

**Core Purpose**
- Executes synthetic experiments to reproduce the Phase Transition Curve (PTC) for Matrix Completion as shown in Figure 2 of the BiG-AMP paper, validating recovery performance across varying sampling and rank ratios.

**Public Interface**
- Function `run_synthetic_ptc()`: Orchestrates the generation of low-rank synthetic data, executes the BiG-AMP solver via the `MatrixCompletion` app, and generates a success/failure heatmap plot. -> `None`: Saves plot to `synthetic_ptc.png`.

**Internal Dependencies**
- From `big_amp_repro.apps.matrix_completion`: `MatrixCompletion` (High-level solver for MC).
- From `big_amp_repro.utils.metrics`: `compute_nmse`, `is_success` (Performance evaluation).
- External packages: `numpy` (Matrix generation and math), `matplotlib.pyplot` (Visualization).

**External Dependencies**
- Expected to be imported by: `big_amp_repro/main.py` (as a subcommand or menu option).
- Key exports used elsewhere: `run_synthetic_ptc`.

**Implementation Notes**
- **Rank Calculation**: Derives the inner dimension $N$ from the rank ratio $\rho$ and sampling ratio $\delta$ using the quadratic formula $N^2 - (M+L)N + \rho|\Omega| = 0$ to ensure the degrees of freedom match the experimental grid.
- **Success Criteria**: Defines success as achieving an NMSE of less than -60dB, consistent with the "noise-free" recovery phase transitions described in the paper.
- **EM Integration**: Uses `use_em=True` in the `MatrixCompletion` wrapper to demonstrate the algorithm's ability to learn noise and prior variances automatically during the transition.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/experiments/run_movielens.py; ROUND 19 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:13:44
**File Implemented**: big_amp_repro/experiments/run_movielens.py

**Core Purpose**:
- Reproduces the MovieLens 100k matrix completion experiment (Figure 7 of the paper), evaluating BiG-AMP's performance using a Laplacian noise model to handle rating outliers.

**Public Interface**:
- Function `download_movielens()`: Downloads and extracts the MovieLens 100k dataset from GroupLens; falls back to synthetic data generation on failure -> `str | None`: Path to the data file.
- Function `load_movielens(file_path)`: Parses the dataset into a rating matrix and a boolean observation mask -> `tuple(np.ndarray, np.ndarray)`: (Y matrix, mask).
- Function `run_movielens_experiment()`: Orchestrates the full experiment: data loading, 90/10 train-test splitting, model fitting via `MatrixCompletion`, and NMAE evaluation.

**Internal Dependencies**:
- From `big_amp_repro.apps.matrix_completion`: `MatrixCompletion` (used to solve the MC problem).
- From `big_amp_repro.utils.metrics`: `compute_nmae` (used for performance validation).

**External Dependencies**:
- `numpy`: Matrix manipulation and random sampling.
- `urllib.request` / `zipfile`: Dataset acquisition and extraction.

**Implementation Notes**:
- **Noise Modeling**: Specifically configures the `MatrixCompletion` app with `noise_type='laplacian'` to match the paper's approach for the MovieLens dataset.
- **Robustness**: Includes a synthetic data generator within the loader to ensure the script remains executable even if external download links are broken.
- **Evaluation**: Compares the resulting Test NMAE against the paper's reported range (~0.185 - 0.190).

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/experiments/run_video_rpca.py; ROUND 20 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:14:02
**File Implemented**: big_amp_repro/experiments/run_video_rpca.py

**Core Purpose**:
- Implements the reproduction script for the Video Robust PCA (RPCA) experiment (Fig 11), demonstrating the separation of a video sequence into a low-rank background and a sparse foreground using the BiG-AMP framework.

**Public Interface**:
- Function `generate_synthetic_video(height, width, num_frames, rank, sparsity)`: Creates a synthetic dataset consisting of a static background and a moving square object to simulate video surveillance. -> `tuple`: Returns (Observations `Y`, True Background `L_true`, True Foreground `S_true`, Dimensions `(h, w)`).
- Function `run_video_rpca_experiment()`: Orchestrates the full experiment workflow, including data generation, model initialization, fitting, NMSE calculation, and result visualization. -> `None`.

**Internal Dependencies**:
- From `big_amp_repro.apps.robust_pca`: `RobustPCA` (The application wrapper for the RPCA problem).
- From `big_amp_repro.utils.metrics`: `compute_nmse` (Used to evaluate recovery quality).

**External Dependencies (Consumers)**:
- Expected to be imported by: `big_amp_repro/main.py` as the primary execution entry for the RPCA demonstration.

**Implementation Notes**:
- **Data Scaling**: Uses a reduced resolution (64x80) compared to the paper's Mall sequence to ensure the reproduction script runs efficiently while still demonstrating the algorithm's effectiveness.
- **Augmented Formulation**: Leverages the `RobustPCA` app which internally handles the augmented matrix logic $Y = [A \mid I][X; S]^T$ to separate the components.
- **Visualization**: Generates a side-by-side comparison of true vs. recovered components for a specific frame and saves it as `video_rpca_results.png`.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/main.py; ROUND 21 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:14:16
**File Implemented**: big_amp_repro/main.py

**Core Purpose**
- The central entry point for the BiG-AMP reproduction suite, providing a command-line interface (CLI) to execute synthetic experiments, real-world benchmarks (MovieLens, Video RPCA), and dictionary learning demonstrations.

**Public Interface**
- `main()`: Parses command-line arguments and routes execution to the appropriate experiment or application module.
- `run_dict_learning_demo()`: A standalone demonstration function that generates synthetic sparse data and recovers it using the `DictLearning` app, printing NMSE and correlation metrics.

**Internal Dependencies**
- From `big_amp_repro.experiments.run_synthetic_ptc`: `run_synthetic_ptc`
- From `big_amp_repro.experiments.run_movielens`: `run_movielens_experiment`
- From `big_amp_repro.experiments.run_video_rpca`: `run_video_rpca_experiment`
- From `big_amp_repro.apps.dict_learning`: `DictLearning`
- From `big_amp_repro.utils.metrics`: `compute_nmse`
- External packages: `argparse` (CLI parsing), `numpy` (data generation/manipulation), `sys` (system operations).

**External Dependencies**
- This is the top-level execution script intended to be run directly from the terminal (e.g., `python -m big_amp_repro.main ptc`).

**Implementation Notes**
- **Architecture decisions**: Uses `argparse` subparsers (`ptc`, `movielens`, `video`, `dict`) to provide a clean, modular interface for different paper sections.
- **Cross-File Relationships**: Acts as the orchestrator that ties together the `apps/` (high-level logic), `experiments/` (execution scripts), and `utils/` (evaluation) modules.
- **Demo Logic**: The `run_dict_learning_demo` serves as a functional test for the Dictionary Learning implementation (Section VIII of the paper), verifying recovery of sparse coefficients and dictionary atoms.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File big_amp_repro/requirements.txt; ROUND 22 
================================================================================

# Code Implementation Summary
**Generated**: 2025-12-24 18:14:26
**File Implemented**: big_amp_repro/requirements.txt

**Core Purpose**:
- Specifies the external Python packages and version constraints required to execute the BiG-AMP framework, run the synthetic experiments, and visualize the results.

**Public Interface**:
- N/A (Standard configuration file for `pip` package manager).

**Internal Dependencies**:
- N/A

**External Dependencies**:
- **numpy** (>=1.21.0): Used for high-performance vectorized matrix operations in the core engine and estimators.
- **scipy** (>=1.7.0): Used for sparse matrix support in Matrix Completion and SVD operations in Rank Logic.
- **matplotlib** (>=3.4.0): Used for generating Phase Transition Curves and visualizing video frames in Robust PCA.

**Implementation Notes**:
- The versions specified ensure compatibility with the array broadcasting and linear algebra routines used throughout the `core/` and `utils/` modules.
- These dependencies are sufficient for CPU-based execution; for GPU acceleration, users would manually substitute `numpy` with `cupy`.

---
*Auto-generated by Memory Agent*


