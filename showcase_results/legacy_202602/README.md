# Legacy 202602 Showcase

这里保留旧 `main` 展示分支里的 warm-start 和 Onsager 图。它们早于当前 schema v6 metric contract，尤其是 `Q_Y` 的展示语义和现在以 `Q_Y_COS_mean` 为主的展示方式不同。

这些图只作为历史参考保留，不建议和当前 `01_qy_cosine_algorithm_comparison`、`02_n_sweep_qy_cosine`、`03_m_sweep_qy_cosine` 的曲线做直接数值比较。
