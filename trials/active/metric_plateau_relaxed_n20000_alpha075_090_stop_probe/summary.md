# metric_plateau_relaxed_n20000_alpha075_090_stop_probe

用途：用 relaxed self-convergence stop 诊断 `N=20000, M=50, S=1, alpha=0.75/0.90` 的停止步数。

该 trial 不是正式 sweep。reference 步数在本 run 完成后按实际 stop step 的 1.5 倍生成。
