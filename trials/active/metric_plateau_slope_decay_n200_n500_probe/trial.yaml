key: metric_plateau_slope_decay_n200_n500_probe
status: active
runtime_class: quick
purpose: Export per-check latent overlap history for N=200/500 slope-decay diagnostics.
owner_area: bigamp_onsager
config: trials/active/metric_plateau_slope_decay_n200_n500_probe/config.yaml
artifact_policy: ignored_workspace
output_root: artifacts/trials/metric_plateau_slope_decay_n200_n500_probe
expected_metrics:
  - Q_W_mean
  - Q_X_mean
notes: Set MF_METRIC_PLATEAU_EXPORT_HISTORY=1 when running; high patience prevents diagnostic early stop.
