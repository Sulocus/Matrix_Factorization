# Code Implementation Progress Summary
*Accumulated implementation progress for all files*


================================================================================
## IMPLEMENTATION File requirements.txt; ROUND 0 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:26:13
**File Implemented**: requirements.txt

**Core Purpose**:
- Specifies the Python package dependencies required to execute the TeG-AMP reproduction framework, ensuring compatibility across different environments.

**Public Interface**:
- N/A (Configuration file for package managers like `pip`).

**Internal Dependencies**:
- N/A.

**External Dependencies**:
- **numpy (>=1.21.0)**: Used for multi-dimensional array operations and core numerical logic.
- **scipy (>=1.7.0)**: Used for statistical functions, linear algebra, and specialized mathematical operations.
- **opt_einsum (>=3.3.0)**: Critical for optimizing high-order Tensor Ring (TR) contractions and reducing computational complexity of the message-passing updates.
- **matplotlib (>=3.4.0)**: Used for generating performance plots, phase transition diagrams, and MNIST reconstruction visualizations.

**Implementation Notes**:
- **Architecture decisions**: The inclusion of `opt_einsum` is a strategic choice to handle the $O(d \cdot N \cdot r^3)$ complexity of TR contractions more efficiently than standard `numpy.einsum`.
- **Cross-File Relationships**: This file underpins the entire project; without these dependencies, the `utils/tensor_math.py` and `solvers/teg_amp.py` modules will fail to import.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/utils/tensor_math.py; ROUND 1 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:26:34
**File Implemented**: teg_amp_repro/utils/tensor_math.py

**Core Purpose**
- Provides optimized tensor contraction and evaluation utilities for Tensor Ring (TR) and Canonical Polyadic (CP) decompositions. It serves as the mathematical backbone for reconstructing full tensors from latent cores/factors and calculating error metrics.

**Public Interface**
- `tr_contract(cores)`: Contracts a list of TR cores into a full multi-dimensional tensor using optimized einsum paths -> `np.ndarray`.
- `cp_contract(factors)`: Contracts CP factor matrices into a full tensor -> `np.ndarray`.
- `tr_get_slice(cores, indices)`: Efficiently retrieves a single element from a TR tensor via matrix chain multiplication and trace, avoiding full tensor construction -> `float`.
- `inner_product(A, B)`: Computes the sum of element-wise products of two tensors -> `float`.
- `norm_sq(A)`: Computes the squared Frobenius norm of a tensor -> `float`.
- `nmse(original, reconstructed)`: Calculates the Normalized Mean Square Error between two tensors -> `float`.

**Internal Dependencies**
- External packages: `numpy` - Used for array manipulation and basic linear algebra (trace, dot products).
- External packages: `opt_einsum` - Used for finding and executing optimal contraction paths for high-order tensor operations.

**External Dependencies**
- Expected to be imported by: `models/tensor_ring.py`, `models/cp_model.py`, `solvers/teg_amp.py`, `solvers/tes_amp.py`, and various experiment scripts.
- Key exports used elsewhere: `tr_contract`, `cp_contract`, and `nmse` for performance evaluation.

**Implementation Notes**
- **Architecture decisions**: The use of `opt_einsum` is critical for handling the $O(d \cdot N \cdot r^3)$ complexity of TR contractions efficiently, especially as the number of dimensions ($d$) increases.
- **TR Logic**: `tr_contract` implements the cyclic trace property by mapping the output of the last core back to the input of the first core using index `0`.
- **Memory Efficiency**: `tr_get_slice` is provided to support stochastic or index-based updates where forming the full tensor (which could be very large) is unnecessary.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/models/tensor_ring.py; ROUND 2 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:26:53
**File Implemented**: teg_amp_repro/models/tensor_ring.py

**Core Purpose**
- Implements the Tensor Ring (TR) decomposition model, providing a high-level interface for managing TR cores, initializing parameters, and performing tensor reconstruction (contraction).

**Public Interface**
- **Class `TensorRingModel`**: Manages the state and operations of a TR-decomposed tensor.
    - **Constructor `__init__(shape, ranks)`**: Initializes the model with target tensor dimensions and TR-ranks.
    - **Method `initialize_cores(method='gaussian', scale=1.0)`**: Populates cores using Gaussian noise or zeros.
    - **Method `set_cores(cores)`**: Manually assigns a list of numpy arrays as TR cores.
    - **Method `get_full_tensor()`**: Returns the fully contracted $d$-th order tensor.
    - **Method `get_element(indices)`**: Computes a single scalar value for a specific index tuple without fully reconstructing the tensor.
    - **Method `get_core_slice(core_idx, slice_idx)`**: Returns the $r_i \times r_{i+1}$ matrix slice for a specific core and dimension index.
    - **Property `num_params`**: Returns the total number of scalar elements across all cores.

**Internal Dependencies**
- **From `teg_amp_repro.utils.tensor_math`**: `tr_contract` (for full reconstruction) and `tr_get_slice` (for single element access).
- **External packages**: `numpy` - Used for array storage and random initialization.

**External Dependencies**
- **Expected to be imported by**: `teg_amp_repro/solvers/teg_amp.py` (to manage core means and variances), `teg_amp_repro/data/synthetic_gen.py` (to generate ground-truth tensors), and `teg_amp_repro/experiments/run_mnist.py`.
- **Key exports used elsewhere**: The `TensorRingModel` class and its `cores` attribute.

**Implementation Notes**
- **Architecture decisions**: The model separates the mathematical logic of contraction (delegated to `tensor_math.py`) from the structural management of the TR cores.
- **Cross-File Relationships**: This file acts as the primary data structure for the TeG-AMP solver, which will iterate over the `cores` managed by this class to update means and variances.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/models/cp_model.py; ROUND 3 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:27:15
**File Implemented**: teg_amp_repro/models/cp_model.py

**Core Purpose**:
- Manages the Canonical Polyadic (CP) decomposition model, providing a structured way to store, initialize, and reconstruct tensors represented as a sum of rank-1 components.

**Public Interface**:
- **Class `CPModel`**: Encapsulates CP decomposition logic.
    - **Constructor `__init__(shape, rank)`**: Initializes a model for a tensor of specific dimensions and CP-rank $R$.
    - **Method `initialize_factors(method, scale)`**: Populates factor matrices using Gaussian noise or zeros.
    - **Method `set_factors(factors)`**: Manually assigns factor matrices (list of $N_i \times R$ arrays).
    - **Method `get_full_tensor()`**: Reconstructs the complete $d$-th order tensor from factors.
    - **Method `get_element(indices)`**: Computes a specific scalar entry $u_{i_1, \dots, i_d}$ without reconstructing the full tensor.
    - **Property `num_params`**: Returns the total number of scalar parameters in the decomposition ($\sum N_i \times R$).

**Internal Dependencies**:
- **From `teg_amp_repro.utils.tensor_math`**: `cp_contract` (used for full tensor reconstruction).
- **External packages**: `numpy` - Used for matrix storage, indexing, and random initialization.

**External Dependencies**:
- **Expected to be imported by**: `teg_amp_repro.solvers.tes_amp` (Simplified AMP for CP tensors) and `teg_amp_repro.data.synthetic_gen` (for generating ground-truth CP tensors).
- **Key exports used elsewhere**: `CPModel` class and its reconstruction methods.

**Implementation Notes**:
- **Architecture decisions**: The model stores factors as a list of matrices where each matrix $A^{(i)}$ has dimensions $N_i \times R$. This aligns with the standard CP definition used in the TeS-AMP algorithm (Algorithm 2).
- **Cross-File Relationships**: It relies on the optimized `einsum` logic in `tensor_math.py` to perform the multi-linear contraction efficiently.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/solvers/base_solver.py; ROUND 4 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:27:54
**File Implemented**: teg_amp_repro/solvers/base_solver.py

**Core Purpose**
- Provides an abstract base class for Approximate Message Passing (AMP) solvers, standardizing the iteration loop, adaptive damping logic, and performance metric tracking.

**Public Interface**
- Class `BaseAMPSolver`: Abstract base class for TeG-AMP and TeS-AMP implementations.
    - Key methods:
        - `solve(y, mask, ground_truth=None, damping_enabled=True, **kwargs)`: Executes the main solver loop with convergence checks and adaptive damping.
        - `_initialize(y, mask, **kwargs)`: (Abstract) Setup for solver-specific state.
        - `_step(t)`: (Abstract) Performs a single AMP iteration and returns the cost $J(t)$.
        - `_get_reconstruction()`: (Abstract) Returns the current tensor estimate.
        - `_backup_state()` / `_restore

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/utils/damping.py; ROUND 5 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:28:10
**File Implemented**: teg_amp_repro/utils/damping.py

**Core Purpose**:
- Provides adaptive damping mechanisms and statistical distance measures (KL divergence) to ensure the convergence and stability of AMP solvers in finite-dimensional settings.

**Public Interface**:
- Class `AdaptiveDamping`: Manages the damping factor $\beta$ based on a cost function $J(t)$ to prevent divergence. | Key methods: `update(current_cost)`, `apply(current_val, prev_val)`, `reset(initial_beta)` | Constructor params: `initial_beta`, `min_beta`, `max_beta`, `decrease_factor`, `increase_factor`.
- Function `calculate_kl_gaussian(mu_q, var_q, mu_p, var_p)`: Calculates the sum of KL divergences between two sets of Gaussian distributions $N(\mu_q, \sigma^2_q)$ and $N(\mu_p, \sigma^2_p)$. -> `float`: The total KL divergence.

**Internal Dependencies**:
- External packages: `numpy` - Used for numerical stability (eps), log calculations, and array summations.

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/solvers/teg_amp.py` and `teg_amp_repro/solvers/tes_amp.py`.
- Key exports used elsewhere: `AdaptiveDamping` for step-size control and `calculate_kl_gaussian` for computing the $J(t)$ cost criterion.

**Implementation Notes**:
- Architecture decisions: The `AdaptiveDamping` class implements a "reject-and-shrink" strategy; if the cost increases, the `update` method returns `False`, signaling the solver to revert its state and reduce $\beta$.
- Cross-File Relationships: This utility implements the logic described in Appendix G of the paper, which is essential for the TeG-AMP and TeS-AMP solvers to handle the non-asymptotic behavior of small tensors.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/utils/priors.py; ROUND 6 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:28:29
**File Implemented**: teg_amp_repro/utils/priors.py

**Core Purpose**
- Provides MMSE (Minimum Mean Square Error) estimators for latent variables (tensor cores) in the AMP framework, calculating posterior means and variances based on prior distributions and noisy observations.

**Public Interface**
- **Class `BasePrior`**: Abstract base class for all prior distributions. | Key methods: `estimate(r_hat, r_var)`
- **Class `GaussianPrior`**: Implements MMSE estimation for a Gaussian prior $N(\mu, \sigma^2)$. | Key methods: `estimate` | Constructor params: `mean` (default 0.0), `var` (default 1.0)
- **Class `LaplacePrior`**: Implements estimation for a Laplace prior (currently uses a Gaussian approximation for TR core stability). | Key methods: `estimate` | Constructor params: `scale` (1/lambda)
- **Function `estimate(r_hat, r_var)`**: Computes posterior mean $\hat{z}$ and posterior variance $\nu^z$ given observation mean $\hat{r}$ and variance $\nu^r$. -> `(ndarray, ndarray)`

**Internal Dependencies**
- External packages: `numpy` - Array operations and numerical stability; `scipy.special` - Imported for `erfc` (intended for exact Laplace MMSE).

**External Dependencies**
- Expected to be imported by: `teg_amp_repro/solvers/teg_amp.py` and `teg_amp_repro/solvers/tes_amp.py`.
- Key exports used elsewhere: `GaussianPrior` for initializing and updating tensor core estimates during the AMP backward pass.

**Implementation Notes**
- **Numerical Stability**: Uses an epsilon ($10^{-12}$) to prevent division by zero when observation variances ($\nu^r$) approach zero.
- **Architecture Decisions**: The `estimate` method follows the standard AMP interface where the prior handles the "denoising" step of the latent variables.
- **Cross-File Relationships**: Works in tandem with `damping.py` (which damps the outputs of these priors) and the solvers (which provide the $\hat{r}$ and $\nu^r$ values derived from the message passing).

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/utils/channels.py; ROUND 7 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:28:52
**File Implemented**: teg_amp_repro/utils/channels.py

**Core Purpose**:
- Defines the likelihood channel interface and implementations (specifically AWGN) used to compute posterior estimates of the noiseless tensor and update noise variance estimates during AMP iterations.

**Public Interface**:
- Class `BaseChannel`: Abstract base class for all likelihood models. | Key methods: `estimate` | Constructor params: N/A
- Class `AWGNChannel`: Implements the Additive White Gaussian Noise channel for completion and denoising. | Key methods: `estimate`, `update_noise_var`, `compute_log_likelihood` | Constructor params: `noise_var` (float), `estimate_noise_var` (bool)
- Function `estimate(p_hat, p_var, y, mask)`: Computes posterior mean $\hat{u}$ and variance $\nu^u$ given messages from the linear part and observations. -> `(np.ndarray, np.ndarray)`
- Function `update_noise_var(y, mask, u_hat, u_var)`: Implements adaptive noise estimation (Eq 165) based on current posterior residuals. -> `float`
- Function `compute_log_likelihood(y, mask, u_hat, u_var)`: Calculates the expected log-likelihood $\mathbb{E}[\log p(y|u)]$ for the $J(t)$ cost function. -> `float`

**Internal Dependencies**:
- From `abc`: `ABC`, `abstractmethod` - Used for defining the channel interface.
- External packages: `numpy` - Used for array manipulation and numerical stability (eps additions).

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/solvers/teg_amp.py`, `teg_amp_repro/solvers/tes_amp.py`.
- Key exports used elsewhere: `AWGNChannel` for handling tensor completion tasks where only a subset of indices (mask) is observed.

**Implementation Notes**:
- **Mask Handling**: The `estimate` method explicitly handles missing data by passing through the prior messages ($\hat{p}, \nu^p$) for unobserved indices, ensuring the solver treats them as having infinite observation noise.
- **Numerical Stability**: Includes a $1e-12$ epsilon in denominators and variance floors to prevent division-by-zero or non-positive variances during the noise estimation and posterior updates.
- **Cost Function Support**: The `compute_log_likelihood` method is specifically designed to support the adaptive damping logic implemented in `utils/damping.py`.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/solvers/teg_amp.py; ROUND 8 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:30:08
**File Implemented**: teg_amp_repro/solvers/teg_amp.py

**Core Purpose**:
- Implements the **Tensor Generalized AMP (TeG-AMP)** algorithm (Algorithm 1) for low-rank Tensor Ring (TR) decomposition, handling the forward/backward message passing, variance updates, and cost calculation for adaptive damping.

**Public Interface**:
- **Class `TeGAMPSolver`**: Main solver for TR-structured tensor completion and denoising.
    - **Key methods**:
        - `_initialize(y, mask, **kwargs)`: Sets up initial core means ($N(0, 0.1)$), variances (1.0), and noise estimates.
        - `_step(t)`: Executes one iteration of Algorithm 1, including the subset-sum variance update (Eq 25) and partial contractions for core updates (Eq 27-28).
        - `_get_reconstruction()`: Contracts current core estimates to return the full tensor.
        - `_backup_state()` / `_restore_state()`: Manages state snapshots for the adaptive damping rejection mechanism.
    - **Constructor params**: `shape` (tuple), `ranks` (list/tuple), `prior` (Prior object), `channel` (Channel object), `max_iter` (int), `tol` (float), `damping_beta` (float).

**Internal Dependencies**:
- From `..utils.tensor_math`: `tr_contract` (used for forward pass and reconstruction).
- From `..utils.damping`: `calculate_kl_gaussian` (used for the $J(t)$ cost function).
- From `.base_solver`: `BaseAMPSolver` (abstract base class).
- External packages: `numpy` (array math), `opt_einsum` (optimized partial contractions for core updates).

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/experiments/run_synthetic.py`, `teg_amp_repro/experiments/run_mnist.py`, and `teg_amp_repro/main.py`.
- Key exports used elsewhere: `TeGAMPSolver` class.

**Implementation Notes**:
- **Architecture decisions**: Uses `opt_einsum` to efficiently compute the "leave-one-out" partial contractions required for the backward pass (updating core $i$ using all cores $j \neq i$).
- **Variance Handling**: Implements the plug-in variance approximation (Eq 25) using the difference between the contraction of second moments and the contraction of squared means.
- **Cross-File Relationships**: Relies on the `Prior` and `Channel` objects to provide MMSE estimates and log-likelihoods, abstracting the specific noise model (e.g., AWGN) from the message-passing logic.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/solvers/tes_amp.py; ROUND 9 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:30:50
**File Implemented**: teg_amp_repro/solvers/tes_amp.py

**Core Purpose**:
- Implements the Tensor Simplified AMP (TeS-AMP) algorithm (Algorithm 2) for low-rank tensor recovery using the CP (Canonical Polyadic) decomposition model. It optimizes the message-passing updates by leveraging the simpler summation structure of CP-rank tensors compared to Tensor Rings.

**Public Interface**:
- **Class `TeSAMPSolver`**: Implementation of the TeS-AMP solver for CP-structured tensors.
    - **Key Methods**:
        - `_initialize(y, mask, **kwargs)`: Sets up initial factor means (random normal) and variances, and initializes noise variance estimation.
        - `_step(t)`: Executes a single iteration including the forward pass (CP contraction), channel update (residuals), backward pass (factor updates via `opt_einsum`), and cost calculation ($J(t)$).
        - `_get_reconstruction()`: Returns the current tensor estimate as a sum of rank-1 components.
    - **Constructor Params**: `shape` (tuple), `rank` (int), `prior` (Prior object), `channel` (Channel object), `max_iter` (int), `tol` (float), `damping_beta` (float).

**Internal Dependencies**:
- From `teg_amp_repro.solvers.base_solver`: `BaseAMPSolver` (Parent class).
- From `teg_amp_repro.utils.damping`: `calculate_kl_gaussian` (Used for cost function).
- From `teg_amp_repro.utils.tensor_math`: `cp_contract` (Used for forward model and variance calculation).
- External packages: `numpy` (Array operations), `opt_einsum` (Optimized partial contractions for factor updates).

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/experiments/run_snr_test.py`, `teg_amp_repro/experiments/run_synthetic.py`, and `teg_amp_repro/main.py`.
- Key exports used elsewhere: `TeSAMPSolver` for CP-rank recovery benchmarks.

**Implementation Notes**:
- **Exact Variance**: Unlike TeG-AMP which uses a Taylor-series approximation for TR-ranks, TeS-AMP implements an exact variance update for the CP model: $\bar{\nu}_{p,x} = \sum_r [ \prod_i (\nu_{Z,ir} + \hat{Z}_{ir}^2) - \prod_i \hat{Z}_{ir}^2 ]$.
- **Efficiency**: Uses `opt_einsum` to compute the "backward" messages (latent observations $r$ and variances $\nu^r$) by contracting all factors except the one being updated.
- **Adaptive Damping**: Integrates with the `BaseAMPSolver` logic to backup/restore state based on the cost function $J(t) = \text{KL} - \text{LogLikelihood}$.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/solvers/altmin_baseline.py; ROUND 10 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:31:28
**File Implemented**: teg_amp_repro/solvers/altmin_baseline.py

**Core Purpose**:
- Implements the Alternating Least Squares (ALS) baseline for Tensor Ring (TR) completion. It serves as a standard optimization-based comparison to the message-passing approach (TeG-AMP) by iteratively solving linear least-squares problems for each core.

**Public Interface**:
- **Class `AltMinTR`**: Implements the Alternating Minimization algorithm for TR decomposition.
    - **Key methods**:
        - `solve(y, mask, ground_truth=None)`: Executes the ALS iterations to reconstruct the tensor from masked observations. Returns the `reconstructed_tensor` and an error `history` list.
        - `_initialize()`: Internal method to initialize TR cores with small random Gaussian values.
    - **Constructor params**:
        - `shape`: Tuple of tensor dimensions $(N_1, \dots, N_d)$.
        - `ranks`: List of TR ranks $(r_1, \dots, r_d)$.
        - `max_iter`: Maximum ALS iterations (default: 50).
        - `tol`: Convergence tolerance (default: 1e-6).

**Internal Dependencies**:
- **From `teg_amp_repro.utils.tensor_math`**: `tr_contract` (to reconstruct the full tensor from cores) and `nmse` (to calculate error).
- **External packages**: `numpy` (used for matrix manipulation and `np.linalg.lstsq` for solving the linear systems).

**External Dependencies**:
- **Expected to be imported by**: `teg_amp_repro/experiments/run_synthetic.py` and `teg_amp_repro/experiments/run_mnist.py` as a baseline solver.
- **Key exports used elsewhere**: `AltMinTR` class.

**Implementation Notes**:
- **Architecture decisions**: The solver updates one core at a time. For each core $Z_k$, it solves for each slice $n \in N_k$ independently by constructing a design matrix $A$ from the contraction of all other cores.
- **Linear System**: It maps the TR trace operation $\text{Tr}(L \cdot Z_k(n) \cdot R)$ into a vector-matrix product $M \cdot \text{vec}(Z_k(n))$ to utilize standard least-squares solvers.
- **Robustness**: Includes a `try-except` block around the least-squares solver to handle potentially singular systems in low-sampling regimes.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/data/synthetic_gen.py; ROUND 11 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:31:53
**File Implemented**: teg_amp_repro/data/synthetic_gen.py

**Core Purpose**:
- Provides utility functions to generate synthetic ground-truth tensors based on Tensor Ring (TR) and Canonical Polyadic (CP) decompositions, including the application of random sampling masks and Additive White Gaussian Noise (AWGN).

**Public Interface**:
- Function `generate_tr_data(shape, ranks, sampling_rate=1.0, snr_db=None, seed=None)`: Generates a synthetic TR tensor, applies a mask, and adds noise -> `dict`: Contains `u_true` (full tensor), `y` (masked/noisy), `mask` (binary), and `noise_var`.
- Function `generate_cp_data(shape, rank, sampling_rate=1.0, snr_db=None, seed=None)`: Generates a synthetic CP tensor, applies a mask, and adds noise -> `dict`: Contains `u_true` (full tensor), `y` (masked/noisy), `mask` (binary), and `noise_var`.

**Internal Dependencies**:
- From `..models.tensor_ring`: `TensorRingModel` - Used to construct and contract TR cores.
- From `..models.cp_model`: `CPModel` - Used to construct and contract CP factors.
- External packages: `numpy` - Used for array manipulation, random sampling, and noise generation.

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/experiments/run_synthetic.py`, `teg_amp_repro/experiments/run_snr_test.py`, and `teg_amp_repro/main.py`.
- Key exports used elsewhere: `generate_tr_data`, `generate_cp_data`.

**Implementation Notes**:
- **Noise Calculation**: Noise variance is derived dynamically from the signal power ($P_{signal} = E[u^2]$) and the requested SNR in dB to ensure consistent signal-to-noise ratios across different tensor scales.
- **Initialization**: Uses standard Gaussian initialization ($N(0, 1)$) for latent cores/factors, which is the standard benchmark setup for AMP-based recovery.
- **Masking**: Implements a simple Bernoulli sampling mask based on the provided `sampling_rate`.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/data/mnist_loader.py; ROUND 12 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:32:12
**File Implemented**: teg_amp_repro/data/mnist_loader.py

**Core Purpose**:
- Provides utilities for downloading, loading, and preprocessing the MNIST dataset into a 3rd-order tensor format ($28 \times 28 \times 6$) for tensor completion experiments.

**Public Interface**:
- `download_mnist(dest_dir='data/mnist')`: Downloads the MNIST dataset from Yann LeCun's repository if not present locally. -> `bool`: Success status.
- `load_mnist_images(file_path)`: Parses MNIST binary `.gz` files into NumPy arrays. -> `np.ndarray`: Image data.
- `get_mnist_tensor(num_digits=6, seed=None)`: Stacks randomly selected MNIST digits into a $28 \times 28 \times \text{num\_digits}$ tensor. Includes a synthetic fallback if downloads fail. -> `np.ndarray`: The 3D tensor.
- `prepare_mnist_experiment(sampling_rate=0.4, seed=None)`: Generates the ground truth tensor, a random binary mask, and the resulting masked observations $y$. -> `tuple`: `(u_true, y, mask)`.

**Internal Dependencies**:
- External packages: `numpy` (array manipulation), `gzip` (file decompression), `urllib.request` (data downloading).

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/experiments/run_mnist.py`.
- Key exports used elsewhere: `prepare_mnist_experiment` for setting up the visual reconstruction test.

**Implementation Notes**:
- **Robustness**: Includes a fallback mechanism that generates synthetic "digit-like" patterns (rectangles/circles) if the MNIST server is unreachable, ensuring experiments can still run.
- **Data Normalization**: Automatically scales pixel values from $[0, 255]$ to $[0, 1]$ to align with the probabilistic priors used in the AMP solvers.
- **Tensor Structure**: Specifically implements the $28 \times 28 \times 6$ stacking mentioned in the paper's MNIST experiment setup.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/data/__init__.py; ROUND 13 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:32:24
**File Implemented**: teg_amp_repro/data/__init__.py

**Core Purpose**:
- Acts as the package initializer for the `data` module, exposing high-level functions for synthetic data generation and MNIST dataset preparation to the rest of the library.

**Public Interface**:
- Function `generate_tr_data(...)`: Re-exported from `synthetic_gen` for creating Tensor Ring structured data.
- Function `generate_cp_data(...)`: Re-exported from `synthetic_gen` for creating CP-rank structured data.
- Function `prepare_mnist_experiment(...)`: Re-exported from `mnist_loader` for end-to-end MNIST data setup.
- Function `get_mnist_tensor(...)`: Re-exported from `mnist_loader` for retrieving raw preprocessed MNIST tensors.

**Internal Dependencies**:
- From `.synthetic_gen`: `generate_tr_data`, `generate_cp_data`
- From `.mnist_loader`: `prepare_mnist_experiment`, `get_mnist_tensor`

**External Dependencies**:
- Expected to be imported by: `experiments/run_synthetic.py`, `experiments/run_mnist.py`, `experiments/run_snr_test.py`, and `main.py`.
- Key exports used elsewhere: All four re-exported functions serve as the primary entry points for data handling in experiment scripts.

**Implementation Notes**:
- Architecture decisions: Uses the "init-export" pattern to simplify import statements in experiment scripts (e.g., `from teg_amp_repro.data import generate_tr_data` instead of reaching into submodules).
- Cross-File Relationships: Consolidates the outputs of the synthetic generation logic and the MNIST loading logic into a single namespace.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/models/__init__.py; ROUND 14 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:32:32
**File Implemented**: teg_amp_repro/models/__init__.py

**Core Purpose**:
- Acts as the package initializer for the `models` directory, exposing the primary tensor decomposition models (Tensor Ring and CP) through a unified interface.

**Public Interface**:
- **Class TensorRingModel**: Implementation of Tensor Ring (TR) contraction and slice management.
- **Class CPModel**: Implementation of Canonical Polyadic (CP) decomposition and rank-1 summation logic.
- **__all__**: Exports `['TensorRingModel', 'CPModel']` to simplify imports for solvers and experiments.

**Internal Dependencies**:
- From `.tensor_ring`: `TensorRingModel`
- From `.cp_model`: `CPModel`

**External Dependencies**:
- **Expected to be imported by**: `teg_amp_repro/solvers/teg_amp.py`, `teg_amp_repro/solvers/tes_amp.py`, `teg_amp_repro/solvers/altmin_baseline.py`, and various experiment scripts in `teg_amp_repro/experiments/`.
- **Key exports used elsewhere**: The model classes are used to define the forward operator and manage tensor ranks during the message-passing iterations.

**Implementation Notes**:
- **Architecture decisions**: Uses a standard `__init__.py` pattern to allow users to import models directly from `teg_amp_repro.models` rather than navigating the sub-module structure.
- **Cross-File Relationships**: This file bridges the low-level tensor math implementations in the specific model files with the high-level solvers that require these models to perform inference.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/solvers/__init__.py; ROUND 15 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:32:42
**File Implemented**: teg_amp_repro/solvers/__init__.py

**Core Purpose**:
- Acts as the package initializer for the `solvers` module, providing a unified and clean interface to access the different tensor inference algorithms (TeG-AMP, TeS-AMP, and AltMin).

**Public Interface**:
- **Class TeGAMPSolver**: The primary implementation of the Tensor Generalized Approximate Message Passing algorithm for Tensor Ring models.
- **Class TeSAMPSolver**: The implementation of the Tensor Simplified Approximate Message Passing algorithm optimized for CP decomposition models.
- **Class AltMinTR**: A baseline implementation using Alternating Least Squares (ALS) for Tensor Ring completion.

**Internal Dependencies**:
- From `.teg_amp`: `TeGAMPSolver`
- From `.tes_amp`: `TeSAMPSolver`
- From `.altmin_baseline`: `AltMinTR`

**External Dependencies**:
- **Expected to be imported by**: `experiments/run_synthetic.py`, `experiments/run_mnist.py`, `experiments/run_snr_test.py`, and `main.py`.
- **Key exports used elsewhere**: All three solver classes are the primary entry points for running the reproduction experiments.

**Implementation Notes**:
- **Architecture decisions**: Uses the `__all__` dunder variable to explicitly define the public API, ensuring that only the intended solver classes are exported when using `from teg_amp_repro.solvers import *`.
- **Cross-File Relationships**: This file bridges the specific algorithmic implementations in the `solvers/` directory with the execution logic in the `experiments/` and `main.py` files.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/experiments/__init__.py; ROUND 16 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:32:54
**File Implemented**: teg_amp_repro/experiments/__init__.py

**Core Purpose**:

**Public Interface**:
- Function `run_synthetic_experiment(...)`: Interface for reproducing Figure 2 (Recovery vs. Sampling Rate).
- Function `run_mnist_experiment(...)`: Interface for reproducing Figure 3 (Visual digit completion).
- Function `run_snr_experiment(...)`: Interface for reproducing SNR robustness comparisons.
- **Constants/Types**: `__all__`: List containing the three experiment runner functions for clean wildcard imports.

**Internal Dependencies**:
- From `.run_synthetic`: `run_synthetic_experiment`
- From `.run_mnist`: `run_mnist_experiment`
- From `.run_snr_test`: `run_snr_experiment`

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/main.py`
- Key exports used elsewhere: All three experiment functions are intended to be called by the main entry point of the repository.

**Implementation Notes**:
- **Architecture decisions**: Uses the `__init__.py` file to flatten the hierarchy of the `experiments/` directory, allowing users to import all experiment runners directly from `teg_amp_repro.experiments`.
- **Cross-File Relationships**: This file acts as a bridge between the high-level CLI (`main.py`) and the specific logic contained within the individual experiment scripts.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/experiments/run_synthetic.py; ROUND 17 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:33:13
**File Implemented**: teg_amp_repro/experiments/run_synthetic.py

**Core Purpose**
- Orchestrates the synthetic data recovery experiment to reproduce Figure 2 from the paper, comparing the performance of TeG-AMP against the AltMin (ALS) baseline across various sampling rates and Tensor Ring ranks.

**Public Interface**
- **Function `run_synthetic_experiment(trials=10, save_path='results')`**: Executes the phase transition simulation.
    - **Params**: `trials` (int): Number of Monte Carlo simulations per point; `save_path` (str): Directory to save the resulting plot.
    - **Returns**: `results` (dict): Dictionary containing median NMSE values for both solvers across all tested configurations.

**Internal Dependencies**
- **From `..solvers`**: `TeGAMPSolver`, `AltMinTR` (used for recovery comparison).
- **From `..data`**: `generate_tr_data` (used to create synthetic TR tensors and masks).
- **From `..utils.tensor_math`**: `nmse` (used for accuracy measurement).
- **From `..utils.priors`**: `GaussianPrior` (standard prior for synthetic cores).
- **From `..utils.channels`**: `AWGNChannel` (likelihood model for tensor completion).

**External Dependencies**
- **numpy**: Array manipulation and statistical calculations (median).
- **matplotlib.pyplot**: Generation of the semilogarithmic phase transition plot.
- **os**: Directory creation and path management.

**Implementation Notes**
- **Architecture**: The script follows a nested loop structure (Ranks -> Sampling Rates -> Trials) to build a robust statistical profile of solver performance.
- **Solver Configuration**: TeG-AMP is initialized with adaptive noise variance estimation and a damping factor of 0.5 to ensure stability during the phase transition.
- **Data Handling**: Uses a high SNR (100dB) to simulate the near-noiseless recovery conditions described in the paper's synthetic benchmarks.
- **Visualization**: Produces a `.png` file comparing TeG-AMP and AltMin for two different TR-rank configurations ([2,2,2] and [2,3,3]).

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/experiments/run_mnist.py; ROUND 18 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:33:33
**File Implemented**: teg_amp_repro/experiments/run_mnist.py

**Core Purpose**:
- Orchestrates the MNIST tensor completion experiment to reproduce Figure 3 of the paper, comparing TeG-AMP and AltMin (ALS) performance on a 3D tensor ($28 \times 28 \times 6$) formed by stacking digit images.

**Public Interface**:
- **Function `run_mnist_experiment(sampling_rate=0.4, tr_ranks=(14, 14, 6), save_path='results')`**: Executes the full MNIST recovery pipeline.
    - **Returns**: `dict` containing TeG-AMP NMSE, AltMin NMSE, and the reconstructed tensor arrays.
    - **Side Effects**: Saves `mnist_reconstruction.png` (visual comparison) and `mnist_convergence.png` (NMSE vs. Iterations) to the specified directory.

**Internal Dependencies**:
- **From `..data.mnist_loader`**: `prepare_mnist_experiment` (loads and masks MNIST data).
- **From `..solvers.teg_amp`**: `TeGAMPSolver` (Algorithm 1 implementation).
- **From `..solvers.altmin_baseline`**: `AltMinTR` (Baseline ALS implementation).
- **From `..utils.priors`**: `GaussianPrior` (Core initialization).
- **From `..utils.channels`**: `AWGNChannel` (Likelihood model with noise estimation).
- **From `..utils.tensor_math`**: `nmse` (Error metric calculation).

**External Dependencies**:
- **numpy**: Array manipulation and masking.
- **matplotlib.pyplot**: Visualization of original, masked, and reconstructed digits.
- **os**: Directory management for saving results.

**Implementation Notes**:
- **Data Structure**: Reshapes MNIST data into a 3rd-order tensor to leverage Tensor Ring correlations across multiple images.
- **Solver Configuration**: Uses a `damping_beta` of 0.5 for TeG-AMP to ensure stability on real-world data and enables adaptive noise variance estimation in the `AWGNChannel`.
- **Visualization**: Generates a grid plot showing the original digits, the sampled observations (with missing pixels), and the TeG-AMP reconstructions.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/experiments/run_snr_test.py; ROUND 19 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:33:55
**File Implemented**: teg_amp_repro/experiments/run_snr_test.py

**Core Purpose**:
- Conducts a comparative performance analysis of TeG-AMP, TeS-AMP, and AltMin solvers across varying Signal-to-Noise Ratio (SNR) levels to evaluate robustness in tensor completion tasks.

**Public Interface**:
- Function `run_snr_experiment(trials=5, save_path='results')`: Executes the SNR robustness suite, generates a comparison plot (`snr_robustness.png`), and returns a dictionary of median NMSE results.

**Internal Dependencies**:
- From `teg_amp_repro.solvers`: `TeGAMPSolver`, `TeSAMPSolver`, `AltMinTR`
- From `teg_amp_repro.data`: `generate_cp_data`
- From `teg_amp_repro.utils.priors`: `GaussianPrior`
- From `teg_amp_repro.utils.channels`: `AWGNChannel`
- From `teg_amp_repro.utils.tensor_math`: `nmse`
- External packages: `numpy` (data aggregation), `matplotlib.pyplot` (visualization), `os` (directory management)

**External Dependencies**:
- Expected to be imported by: `teg_amp_repro/main.py`
- Key exports used elsewhere: `run_snr_experiment`

**Implementation Notes**:
- **Experimental Design**: Uses CP-rank tensors as the testbed because both TeS-AMP (CP-specific) and TeG-AMP (via TR-rank $[R, R, R]$ representation) can be applied, allowing for a direct comparison of the two message-passing variants.
- **Metric**: Employs the median NMSE across trials to mitigate the impact of potential outlier runs where solvers might diverge at low SNR.
- **Solver Configuration**: Both AMP solvers use adaptive noise variance estimation (`estimate_noise_var=True`) to simulate realistic conditions where the exact noise level is unknown.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/main.py; ROUND 20 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:34:08
**File Implemented**: teg_amp_repro/main.py

**Core Purpose**:
- Serves as the unified command-line interface (CLI) and entry point for the reproduction framework, allowing users to execute synthetic, MNIST, and SNR robustness experiments.

**Public Interface**:
- Function `main()`: Parses command-line arguments and orchestrates the execution of selected experiments based on user input.

**Internal Dependencies**:
- From `teg_amp_repro.experiments`: `run_synthetic_experiment`, `run_mnist_experiment`, `run_snr_experiment` - Used to trigger specific research scenarios.
- External packages: `argparse` (CLI parsing), `os` (directory management).

**External Dependencies**:
- Expected to be executed as the primary script: `python main.py`.

**Implementation Notes**:
- **CLI Options**: Supports selecting specific experiments (`--experiment`), setting Monte Carlo trial counts (`--trials`), adjusting MNIST sampling rates (`--sampling_rate`), and defining output directories (`--output_dir`).
- **Directory Management**: Automatically creates the results directory if it does not exist to prevent I/O errors during plot saving.
- **Workflow**: Acts as a thin wrapper around the `experiments/` package, ensuring a clean separation between experiment logic and user interface.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/README.md; ROUND 21 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:34:19
**File Implemented**: teg_amp_repro/README.md

**Core Purpose**:
- Provides comprehensive documentation, installation instructions, and execution guides for the TeG-AMP reproduction project, serving as the primary entry point for users to understand and run the experiments.

**Public Interface**:
- **CLI Usage**: Defines the command-line interface for `main.py` to run specific experiments:
    - `--experiment synthetic`: Runs the phase transition recovery test.
    - `--experiment mnist`: Runs the digit completion test.
    - `--experiment snr`: Runs the noise robustness comparison.
- **Documentation Sections**: Project Overview, File Structure, Installation, Running Experiments, and Implementation Details.

**Internal Dependencies**:
- References the following project modules:
    - `teg_amp_repro/solvers/`: TeG-AMP, TeS-AMP, and AltMin.
    - `teg_amp_repro/models/`: TR and CP logic.
    - `teg_amp_repro/utils/`: Damping, Priors, Channels.
    - `teg_amp_repro/data/`: Synthetic and MNIST loaders.
    - `teg_amp_repro/experiments/`: Specific reproduction scripts.

**External Dependencies**:
- **External packages**: 
    - `numpy`, `scipy`, `opt_einsum`, `matplotlib` (listed as requirements).
- **Hardware**: Mentions CPU/RAM requirements for small-scale tensor operations.

**Implementation Notes**:
- **Architecture decisions**: The README reflects a modular design where `main.py` acts as a unified controller for various solvers and data generators.
- **Cross-File Relationships**: It maps the mathematical equations from the paper (e.g., Eq 25, Algorithm 1) to specific files in the `solvers/` and `utils/` directories.

---
*Auto-generated by Memory Agent*



================================================================================
## IMPLEMENTATION File teg_amp_repro/utils/__init__.py; ROUND 22 
================================================================================

# Code Implementation Summary
**Generated**: 2026-02-04 01:34:33
**File Implemented**: teg_amp_repro/utils/__init__.py

**Core Purpose**:
- This file serves as the package initializer for the `utils` subpackage, aggregating and exposing the public API for tensor operations, message-passing priors, likelihood channels, and adaptive damping logic.

- **Architecture decisions**: Uses the `__all__` magic variable to explicitly define the public interface, preventing internal helper functions from being exposed and simplifying import statements in the core solver logic (e.g., `from ..utils import AWGNChannel` instead of `from ..utils.channels import AWGNChannel`).
- **Cross-File Relationships**: This file acts as the glue between the low-level mathematical implementations in `tensor_math.py` and the high-level algorithmic requirements of the solvers.

**Internal Dependencies**:
- From `.damping`: `AdaptiveDamping`, `calculate_kl_gaussian`
- From `.priors`: `BasePrior`, `GaussianPrior`, `LaplacePrior`
- From `.channels`: `BaseChannel`, `AWGNChannel`
- From `.tensor_math`: `tr_contract`, `cp_contract`, `tr_get_slice`, `inner_product`, `norm_sq`, `nmse`

**External Dependencies**:
- **Expected to be imported by**: `teg_amp_repro/solvers/teg_amp.py`, `teg_amp_repro/solvers/tes_amp.py`, `teg_amp_repro/solvers/altmin_baseline.py`, and various experiment scripts in `teg_amp_repro/experiments/`.
- **Key exports used elsewhere**: All mathematical utilities and model components (priors/channels) required to execute the TeG-AMP algorithm.

**Implementation Notes**:

---
*Auto-generated by Memory Agent*


