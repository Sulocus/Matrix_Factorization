# Local Legacy Archive

This directory is for local cleanup archives that should not be part of the
active source tree. Large experiment outputs and caches are ignored by git.

Current archive:

- `2026-05-03_share_cleanup/`: local outputs, caches, and old loose result
  bundles moved out of the active project root before sharing the project.
